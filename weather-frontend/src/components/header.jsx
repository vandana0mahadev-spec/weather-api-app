function Header() {
  return (
    <header className="header">
      <p className="eyebrow">React + Spring Boot + Redis</p>
      <h1>Weather API App</h1>
      <p className="subtitle">
        Search for a city and view live weather data from your backend API.
      </p>
    </header>
  );
}

export default Header;